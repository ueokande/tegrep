# tegrep
Grep manually

![Screenshot of tegrep](./screenshot.gif)

Usage
-----

```sh
command | tegrep
```

or

```sh
tegrep file
```

`tegrep` checks `$EDITOR`.  When `$EDITOR` is not set, `edit` command is used.

License
-------

MIT
